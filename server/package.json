{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8787} --reload",
    "build": "uv sync --frozen && uv run python -m compileall -q app",
    "start": "uv run uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "typecheck": "uv run python -m compileall -q app tests",
    "lint": "uv run ruff check app tests && uv run ruff format --check app tests",
    "format": "uv run ruff format app tests",
    "test": "uv run pytest"
  },
  "dependencies": {},
  "devDependencies": {}
}
